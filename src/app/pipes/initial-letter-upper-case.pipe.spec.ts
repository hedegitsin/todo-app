import { InitialLettersUpperCasePipe } from './initial-letter-upper-case.pipe';

describe('InitialLetterUpperCasePipe', () => {
  it('create an instance', () => {
    const pipe = new InitialLettersUpperCasePipe();
    expect(pipe).toBeTruthy();
  });
});
